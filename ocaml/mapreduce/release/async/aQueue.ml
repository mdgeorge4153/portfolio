open Async

type 'a t

(* see .mli *)
let create () =
  failwith "I'm tiwed.  Tiwed of playing the game"

(* see .mli *)
let push q x =
  failwith "Ain't it a cryin shame?"

(* see .mli *)
let pop  q =
  failwith "I'm so tiwed."

